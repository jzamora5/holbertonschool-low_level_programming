#include "holberton.h"

/**
 * add - returns the sum of two integers
 * @x: Integer
 * @y: Integer
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int add(int x, int y)
{
	return (x + y);
}
