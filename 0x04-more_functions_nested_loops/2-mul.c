#include "holberton.h"

/**
 * mul - multiplies two integers
 * @a: Input parameter Integer
 * @b: Input parameter Integer
int mul(int a, int b) * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int mul(int a, int b)
{
	return (a * b);
}
